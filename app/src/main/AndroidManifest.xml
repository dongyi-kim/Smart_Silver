<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cdmst.smartsilver" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="src.activities.ActMain"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="src.activities.ActStep01"
            android:screenOrientation="portrait"
            >

        </activity>

        <activity android:name="src.activities.ActStep0103">
        </activity>

        <activity android:name="src.activities.ActStep0104">
        </activity>

    </application>

</manifest>
